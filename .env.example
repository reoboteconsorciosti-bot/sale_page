# String de conexão PostgreSQL usada pelo Prisma (driver adapter @prisma/adapter-pg).
DATABASE_URL="postgresql://usuario:senha@host:5432/nome_do_banco?schema=public"

# Segredo usado para assinar o JWT da sessão (Cookie HTTPOnly).
# Gere um valor forte em produção, ex.: openssl rand -base64 32
SESSION_SECRET="troque-por-um-segredo-aleatorio-forte"
DATABASE_URL="postgres://postgres:Postgres123y@82.25.66.22:54/simulador_landingpage?sslmode=disable"